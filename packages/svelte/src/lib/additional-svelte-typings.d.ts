declare namespace svelteHTML {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  interface HTMLAttributes<T> {
    /** Google's auto-mode marker attribute. */
    'google-add-preferred-source-btn'?: string | boolean;
  }
}
