# @opacedev/preferred-source-element

## 1.0.0

### Major changes

- Initial public release of the `<preferred-source-button>` web component with manual and automatic modes, three variants, light and dark themes, CSS custom properties, deeplink fallback and `ps-click`, `ps-ready` and `ps-fallback` events.
